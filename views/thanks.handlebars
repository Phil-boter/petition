{{#if error}}
<div class="error">Oops... Sorry! Something went wrong!</div>
{{/if}}

<div class="container">
    <div class="content">
        <h1>Thank you for signing this petition!</h1>
        <a href="/signers">See all signers{{count}}</a>
        <img class="sig-img" src="{{signature}}" alt="">
    </div>
</div>