<div class="petition">
    <h3>Signers of the Petition</h3>
    <p>We the undersigned demand that people should buy at thier local brewery instead buying international!</p>

    {{#if error}}
    <div class="error">Oops... Sorry! Something went wrong!</div>
    {{/if}}
</div>

<div class="body-container">
    <div class="container" id="signers">
        {{#each arrSigners}}
        <li>
            {{#if url}}
            <p><a href={{url}}>{{first}} {{last}}</a></p>
            {{else}}
            {{first}} {{last}}
            {{/if}}
            {{age}}
            {{#if city}}
            <p><a href="/signers/{{city}}">{{city}}</a></p>
            {{/if}}
        </li>
        {{/each}}
    </div>

    <div class="logout">
        <a href="/logout">LOGOUT</a>
    </div>
</div>